apiVersion: v1
kind: Service
metadata:
  name: hermes-agent
  namespace: hermes-agent
  labels:
    app.kubernetes.io/name: hermes-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hermes-agent
  ports:
    - name: dashboard
      port: 9119
      targetPort: dashboard
    - name: gateway
      port: 8642
      targetPort: gateway
