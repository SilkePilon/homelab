apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cluster-scoped store definition. The External Secrets Operator itself is
# installed separately (Helm) — see the repo README — because it ships CRDs that
# must exist before this resource can be applied.
resources:
  - clustersecretstore.yaml
