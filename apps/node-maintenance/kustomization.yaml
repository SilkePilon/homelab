apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: node-maintenance

resources:
  - namespace.yaml
  - image-gc.yaml

patches:
  - target:
      kind: DaemonSet
    patch: |
      - op: add
        path: /spec/revisionHistoryLimit
        value: 2
